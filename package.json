{
  "name": "csc-3100-app",
  "version": "1.0.0",
  "description": "First individual assignment for Prasi Aravind in CSC 3100",
  "license": "ISC",
  "author": "Prasi Aravind",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run -w react-frontend dev",
    "start": "npm run -w react-frontend build && npm run -w react-frontend preview"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
